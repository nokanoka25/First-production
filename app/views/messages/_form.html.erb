<%= form_with model: [group, Message.new], local: true do |form| %>
    <div class="form-group">
      <%= form.text_field :content, placeholder: "メッセージを入力してください", class: "form-control" %>
    </div>
    <div class="form-group mt-2">
      <%= form.submit "送信", class: "btn btn-primary" %>
    </div>
  <% end %>